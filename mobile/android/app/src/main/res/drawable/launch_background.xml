<?xml version="1.0" encoding="utf-8"?>
<!--
  Launch window for API < 21. Painted in the app's own surface colour rather
  than white, so the first thing on screen is already the app's background and
  not a flash of white before it.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/avto_surface" />
</layer-list>
