// Smoke test — verifies the test harness is functional.
// Real widget and unit tests live in test/car_selector/ and will
// be extended by 03-04 / 03-05.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: test harness is operational', () {
    expect(1 + 1, equals(2));
  });
}
